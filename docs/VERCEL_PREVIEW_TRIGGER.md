# Vercel preview trigger

This branch-only marker exists to trigger the first Vercel preview deployment after GitHub project import. It does not change application behavior and should be removed before the branch is merged to production.
